schema_version = "safedrive.cora.repair_protocol.v1"
dataset_id = "h6-cora-c2-repair-20260906-v3"
base_dataset_id = "h6-cora-c2-dev-20260830-v1"
map = "Town03"
diagnostic_roots = 12
batch_roots = 48
max_batches = 2
max_roots = 108
max_branch_attempts = 432
carla_wall_limit_s = 14400
whole_gpu_peak_limit_gib = 14.5
free_disk_floor_gib = 60.0
dataset_limit_gib = 20.0
multipliers = [1.0, 2.0, 3.0]
diagnostic_seed_start = 270000
train_seed_start = 271000
validation_seed_start = 272000
calibration_seed_start = 273000
locked_development_seed_start = 274000
min_diagnostic_repair_failure_roots = 2
min_diagnostic_offroad_roots = 1
base_verification = "REUSE_RECORDED_IDENTITIES_NO_OLD_FILE_HASH_SCAN"

# These recipes are selected only from the train-only screening artifact.
# Each entry is applied to the named source; an empty source list is a
# deliberate NOT_SELECTED result and is preserved in the root record.
[recipes]
diagnostic_repair_failure = [
  { source = "vla", operator = "speed_scale_up", multiplier = 3.0 },
  { source = "vla", operator = "delayed_brake", multiplier = 2.0 },
]
diagnostic_offroad = [
  { source = "expert", operator = "lateral_offset_toward_conflict", multiplier = 3.0 },
  { source = "vla", operator = "lateral_offset_toward_conflict", multiplier = 3.0 },
]
formal_repair_failure = [
  { source = "vla", operator = "speed_scale_up", multiplier = 3.0 },
  { source = "vla", operator = "delayed_brake", multiplier = 2.0 },
]
formal_offroad = [
  { source = "expert", operator = "lateral_offset_toward_conflict", multiplier = 3.0 },
  { source = "vla", operator = "lateral_offset_toward_conflict", multiplier = 3.0 },
]
