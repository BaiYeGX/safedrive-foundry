# Codex任务启动入口

> 用户每次只需指定任务ID并要求读取本文件，例如：`执行G1-01，读取START_TASK.md。`

本文件只负责启动路由，不重复定义执行规则。根目录`AGENTS.md`是唯一执行协议，`ROADMAP.md`是任务和依赖的唯一来源，`PROGRESS.md`是动态状态的唯一来源。

## 1. 接收任务ID

从用户指令提取严格格式`GX-XX`，例如`G1-01`、`G5-03`。

- 用户没有给出任务ID时，不猜测；读取`PROGRESS.md`并报告推荐任务，等待明确指令。
- 用户给出多个任务ID时，只执行第一个，其他任务不得自动开始。
- 用户要求恢复时，仍按同一任务ID定位，并优先读取任务末尾断点。

## 2. 固定读取两项

1. 读取`PROGRESS.md`。
2. 在`tasks/GX/`中查找唯一匹配`GX-XX_*.md`的任务文件并完整读取。

匹配结果必须恰好一个：

- 0个：停止并报告任务不存在；
- 多个：停止并标记`DECISION_REQUIRED`，不得自行选择；
- 1个：继续依赖检查。

## 3. 检查依赖

以当前任务文件的`依赖`字段执行快速检查；只有依赖不清、任务编号冲突或阶段切换时，才读取`ROADMAP.md`对应阶段核对。

- 前置任务未完成：记录`BLOCKED`并停止；
- 前置任务完成：将当前任务登记为`CURRENT/IN_PROGRESS`后执行；
- G0保持冻结，后续任务不得回写G0任务或证据。

### 3.1 真实 CARLA 任务固定预检

当前任务文件明确依赖真实 CARLA 时，先执行已经实现并验证的统一入口：

```text
sdf sim preflight
```

结果处理：

1. `READY`：继续当前任务。
2. `RETRYABLE_FAILURE`：执行一次 `sdf sim ensure`；若 ensure 成功，只重新执行一次 `sdf sim preflight`，通过后继续。
3. `ensure` 返回 `needs_user_action=true` 且 `error_code=NEEDS_USER_ACTION`：将任务标记 `BLOCKED_EXTERNAL`，写出最小用户操作并停止。
4. 版本不匹配、tick owner 冲突或依赖冲突：标记 `BLOCKED` 或 `DECISION_REQUIRED`，不得自动换版本。
5. 不依赖真实 CARLA 的任务不强制执行该预检。

任务不得自行写死 CARLA IP、复制历史 gateway、source `carla_environment.sh` 作为业务前置、复制连接重试或自行决定 tick master。只有已实现且已验证的命令才允许写入本入口。

### 3.2 管理文档一致性维护例外

用户明确要求“任务目录与管理文档全量一致性修复”时，该请求是仓库维护任务，不启动任何 G0～G8 路线任务，也不改变当前路线任务状态。维护范围仅包括目录/依赖/结构审计、管理文档最小修复、维护检查器、维护测试和审计报告；G0 任务、配置、代码与历史证据保持冻结。维护任务完成后停止，等待用户明确指定路线任务。

## 4. 按需读取路由

只读取命中的条目，不通读全部管理文档：

| 当前任务情况 | 额外读取 |
|---|---|
| 任务文件明确引用某文档、接口、配置、断点或产物 | 只读被引用内容 |
| 涉及时间、身份、消息接口、Profile、Oracle/Observable、Registry、组件权限 | `docs/project/EXECUTION_ARCHITECTURE.md`相关章节 |
| G1-04～G2-06 | `docs/project/decisions/CLASSIC_ALGORITHM_OPTIMIZATION.md` |
| 阶段验收、消融、量化结论、Evidence或简历数字 | `docs/project/CLAIMS.md` |
| 需要直接依赖的接口或失败证据 | 直接依赖任务的最终断点及其明确产物 |
| 需求冲突或重大范围变更 | `FUTURE_PROJECT_VISION.md`对应章节 |

不得因为文件可能有用就读取整份总需求、完整历史任务或所有专项决策。

## 5. 执行与停止

严格按当前任务的允许修改范围、完成标准和验证方法执行。结束前必须：

1. 运行规定验证；
2. 在任务文件追加命令、结果、修改文件、失败和精确恢复步骤；
3. 更新`PROGRESS.md`的当前指针、阻塞、最近完成和推荐下一任务；
4. 只有全部标准满足才标记`COMPLETED`；
5. 向用户汇报`GX-XX已完成`或未解决问题；
6. 停止，不自动读取或开始下一任务。

## 6. 外部阻塞时立即交接

如果遇到权限、沙盒、管理员/UAC、GUI、登录、重启、许可证、网络凭据、外部服务，或 Codex 完全无法完成的交互操作：

1. 只做一次最小只读诊断；
2. 记录错误原文和已尝试命令；
3. 将任务标记为`BLOCKED_EXTERNAL`、`BLOCKED`或`DECISION_REQUIRED`；
4. 在任务断点和`PROGRESS.md`写出用户需要手动执行的最小步骤；
5. 停止并等待用户接管。

同一阻塞原因连续两次出现时不得继续重试。不得自动换路径、扩大权限、修改系统配置、删除数据或开始下一任务。用户完成手动步骤并明确要求恢复后，继续原任务。

## 7. 用户固定口令

### 开始

```text
执行G1-01，读取START_TASK.md。
```

### 恢复

```text
恢复G1-01，读取START_TASK.md。
```

### 继续解决问题

```text
继续解决G1-01，读取START_TASK.md；不要开始下一任务。
```
