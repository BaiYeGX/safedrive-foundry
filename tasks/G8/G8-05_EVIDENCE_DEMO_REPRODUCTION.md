# G8-05：证据包、演示与一键复现

- **状态**：`PENDING`
- **依赖**：`G8-02`、`G8-03`、`G8-04`
- **目标**：把原始运行、聚合指标、图表、演示视频和复现入口绑定到同一证据谱系，形成可审计的项目交付物。
- **范围**：证据清单、结果卡、演示脚本、固定种子复现、失败案例回放、简历数字来源索引。
- **允许修改**：`docs/evidence/**`、`safedrive_foundry/artifacts/g8/reports/**`、`scripts/reproduce/**`、演示与发布配置；不得回写或挑选性删除原始结果。
- **完成标准**：关键主张均能追溯到协议、配置、提交/快照、原始日志和聚合脚本；全新工作目录可按文档复现代表性结果。
- **验证方法**：执行证据谱系检查、冷启动复现和演示彩排；核对资产哈希与结果卡。

## 交付物

- 本任务范围内的实现或配置、对应测试/场景、运行记录和可追溯报告；具体对象以本文件的范围和完成标准为准。

## 资源与自动化边界

- 仅使用本任务允许修改路径、已登记输入和版本化配置，不启动后续任务。涉及真实 CARLA 时先执行 `sdf sim preflight`；不得自行解析 WSL gateway、创建第二套 `carla.Client`/tick master 或由业务节点直接调用 `world.tick()`。Agent 不执行任意 shell，学习模块不得修改 Safety 硬约束，MCP 保留到 G7-01。
## 断点记录

- 尚未开始。恢复时先核对 G8-02～G8-04 的冻结结果和 `docs/project/CLAIMS.md`。
