# G8-06：最终验收与可发布版本

- **状态**：`PENDING`
- **依赖**：`G8-05`
- **目标**：完成全系统最终门禁，输出可复现、可演示、可答辩且不夸大结论的发布候选版本。
- **范围**：需求追踪、全量回归、资源预算、已知限制、模型卡/系统卡、发布清单和项目叙事。
- **允许修改**：`safedrive_foundry/validation/g8/**`、`safedrive_foundry/artifacts/g8/**`、发布、文档与证据索引；只允许修复验收发现的发布阻塞缺陷，不得临时扩大实验范围。
- **完成标准**：G0～G8 阶段条件全部满足；C1～C6 主张有证据或被明确降级；单机复现路径、演示路径和已知限制完整。
- **验证方法**：执行最终验收清单、全量回归、文档链接/哈希检查和一次从零复现抽检。

## 交付物

- 本任务范围内的实现或配置、对应测试/场景、运行记录和可追溯报告；具体对象以本文件的范围和完成标准为准。

## 资源与自动化边界

- 仅使用本任务允许修改路径、已登记输入和版本化配置，不启动后续任务。涉及真实 CARLA 时先执行 `sdf sim preflight`；不得自行解析 WSL gateway、创建第二套 `carla.Client`/tick master 或由业务节点直接调用 `world.tick()`。Agent 不执行任意 shell，学习模块不得修改 Safety 硬约束，MCP 保留到 G7-01。
## 断点记录

- 尚未开始。恢复时从发布候选清单和最近一次失败门禁继续。
