# Codex 执行规则

本文件适用于项目根目录及全部子目录。`FUTURE_PROJECT_VISION.md` 是不可随意修改的总需求；任务执行以 `ROADMAP.md`、`PROGRESS.md` 和指定任务文件为准。

## 1. 单任务原则

1. 每次 Codex 对话只能执行一个由用户明确指定、或 `PROGRESS.md` 中标记为 `CURRENT` 的任务。
2. 未完成当前任务时，不得自动开始下一任务。
3. 完成当前任务后，只能更新测试结果、任务断点记录和 `PROGRESS.md`，随后停止并等待用户指令。
4. 若用户的新指令与当前任务冲突，以用户最新明确指令为准，并在进度文件记录中断原因。
5. G0～G8任务已经预先拆分；只能执行依赖已满足的当前指定任务，不得因后续任务文件已存在而跳阶段实施。

## 2. 开始任务前

Codex 必须依次读取：

1. `FUTURE_PROJECT_VISION.md` 中与当前阶段有关的内容；
2. `ROADMAP.md`；
3. `PROGRESS.md`；
4. 当前任务 `.md` 文件；
5. 当前任务直接引用的决策、配置或断点文件。

确认依赖任务已完成；若未完成，将当前任务标记为 `BLOCKED`，不得绕过依赖继续。

## 3. 执行边界

- 只修改当前任务“允许修改”范围内的文件。
- 不删除用户数据，不覆盖归档，不擅自升级已冻结版本。
- 需要管理员权限、重启、登录、许可接受或 GUI 操作时，记录为 `BLOCKED_EXTERNAL` 并向用户给出最小操作说明。
- 不把未测量结果写成已完成，不把预期指标写成真实指标。
- 发现总需求冲突时停止扩张范围，在任务与进度文件中记录 `DECISION_REQUIRED`。

## 4. 完成任务的强制步骤

1. 执行任务文件规定的验证方法。
2. 记录执行过的命令、结果摘要和未解决问题。
3. 更新任务文件的“断点记录”和最终状态。
4. 更新 `PROGRESS.md` 的当前阶段、任务状态、阻塞问题和推荐下一任务。
5. 向用户汇报结果并停止，不自动启动下一任务。

任务只有在全部完成标准满足且验证通过时才能标记 `COMPLETED`。部分完成应标记 `PAUSED` 或 `BLOCKED`。

## 5. 断点恢复

每个任务文件末尾包含断点记录。对话即将中断、额度不足或工作尚未完成时，必须追加：

- 时间；
- 当前状态；
- 已完成内容；
- 修改文件；
- 已运行验证；
- 失败或阻塞；
- 精确恢复步骤；
- 下一条建议命令。

恢复时先验证断点所述文件和外部状态仍然成立，再继续，不重复已验证工作。

## 6. 状态枚举

```text
PENDING
CURRENT
IN_PROGRESS
PAUSED
BLOCKED
BLOCKED_EXTERNAL
DECISION_REQUIRED
VALIDATING
COMPLETED
```
