# 学校项目紧急收尾方案（2026-09-11）

状态：分析与执行建议，C4 尚未运行。本轮补算使用全部 53 个 validation roots，没有按
模型表现排除样本。用户明确：**本周日 2026-09-13 结束，老师硬性要求实时 CARLA 闭环**。
按周日中午冻结现场版本、下午排练和最终交付安排；具体提交时刻未指定。实时演示已是
必要交付，离线预测、日志重放和录像只能补充，不能替代现场运行。

当前优先顺序：保留 C3/M1 → 先恢复 CARLA 并跑通 M1 最小闭环 → 完成 C4/M2 →
把 M2/新 World 接入同一演示链 → 少量场景排练与 C6 交付。演示链前置验证不新增研究阶段。

## 周五至周日的执行安排

| 日期 | 主任务 | 当日可检查产物/节点 |
|---|---|---|
| 周五 09-11 | C3 指标与报告收尾；优先恢复 CARLA，接入现有 M1 | 本次全量结果页已生成；争取当晚跑通可重置的 60 s M1 现场闭环 |
| 周六 09-12 | 完成真实 C4 联合 batch 与一次 M2；接入同一现场链 | M2 真实训练/重载/离线表；1–2 个三臂功能对照，记录控制来源与失败 |
| 周日 09-13 上午 | 现场排练和修直接影响演示的错误 | 冷启动、运行、停止重置再次运行；中午冻结模型/场景/配置 |
| 周日 09-13 下午 | C6、答辩材料、复现检查和 GitHub 同步 | 真实现场版本、图表/讲解、备用录像、证据索引及远端 SHA 核对 |

以上是交付安排，不是已经完成或保证按时通过。周五 CARLA 未通时，周六优先继续处理
现场阻塞并同步做不占 GPU 的 C4 代码准备；不能取消实时要求。M2 必须在有效数据和资源
预算确认后运行，不以空辅助或减少未披露更新数凑完成。周日冻结后仅修复演示阻塞，
不追加 seed、数据、模型或按结果调参。功能矩阵缩小则如实标注，不冒称原 C5 完整矩阵。

## 已有成果足以形成主结果

数据源为 `generated/h6/cora/c3-repair-20260910T165902Z/`，使用冻结标签、原 mask 和
保存的 M0/M1 预测，逐 root 计算后等权平均。分组按已有 manifest 的 family/map_name，
为本次事后描述性分析，不是新增独立测试，也不是未见地图泛化实验。

| 分组 | roots | M0 P-WP (m) | M1 P-WP (m) | 相对下降 |
|---|---:|---:|---:|---:|
| 全部 | 53 | 2.178557 | 0.337217 | 84.52% |
| Town01 | 18 | 2.139864 | 0.413649 | 80.67% |
| Town03 | 18 | 1.831026 | 0.493654 | 73.04% |
| Town05 | 17 | 2.587502 | 0.090651 | 96.50% |

| 已有场景类别 | roots | P-WP 相对下降 | route ADE 相对下降（负值为退化） |
|---|---:|---:|---:|
| aggressive_cut_in | 6 | 78.41% | 42.10% |
| cross_traffic_conflict | 6 | 86.41% | 54.48% |
| cut_in | 6 | 78.55% | 42.12% |
| emergency_lead_brake | 6 | 88.02% | -17.24% |
| free_flow | 6 | 84.28% | -169.72% |
| red_light_dilemma | 6 | 90.50% | -64.84% |
| red_light_hold | 5 | 79.69% | -22.03% |
| slow_lead | 6 | 88.39% | -19.87% |
| stopped_lead | 6 | 86.29% | 55.20% |

短时 waypoint 的均值在全部九类和三个地图分组中均下降，52/53 roots 改善。全量 route
ADE 从 1.170243 到 0.749253 m，平均下降 35.97%，但 29/53 roots 退化。free_flow 的
route 从 0.084456 到 0.227792 m，绝对增加 0.143336 m；相对百分比要与绝对值并列解释。
两指标不合成“综合驾驶能力提升”。现有数据全部为零速度 anchor，结论限定当前开发数据。

完整复算与逐组 root 清单保存在本机新目录：

- `generated/h6/cora/c3-closeout-review-20260911T015150Z/recompute_groups.py`
- `generated/h6/cora/c3-closeout-review-20260911T015150Z/group-review.json`
- JSON SHA-256：`0f09830a3780a28fb3d261ceb755dc86eaacc4218ed2f1950609299e48ab6b61`

该脚本未读取生产聚合函数，要求预测行完整、唯一且有限，发现错误即中止而不删除行。
本次首次探索统计标签时遇到 list/dict 类型差异，修正读取方式后确认旧训练清单四个
World 目标各有 158 条被原记录标为有效；这些标记不代替候选/执行初态绑定核验。

## 统计与展示取舍

主结果优先使用已有全量优势，不需要删除表现差的样本。可以展示表现明确的真实案例，
或者额外给出标明筛选方法及 n/53 的事后子集；性能筛选后的结果不冒充全量开发表现。
确有图像损坏、时间错位或标签错误时，对所有模型应用相同质量规则，保留排除清单和原表；
监督变更与单纯报告修正分别处理。主页面简洁，完整结果表保留简单基线与退化。

## C3 最后一次收尾：保留模型，只修会影响数字和接续的问题

指标与呈现已收尾，见 [C3 学校项目结果页](C3_SCHOOL_RESULTS.md)。本次已修独立有效率、
P-WP 单位、导航共同支持、预测完整性和报告覆盖问题，10 项直接测试通过；实际 canonical
转换 M0/M1 均为 53/53，通过独立复算且保留全部样本。资源总账核对仍待完成。

1. 修独立评估：真值 mask 不完整与预测失败分开；P-WP 单位统一为 m；导航与模型使用
   相同支持范围比较。保存新版报告，不覆盖旧实验记录。
2. 补资源账本中 smoke/失败漏项并确认剩余训练预算。已有训练、checkpoint 和 200 次
   更新保留，不能把未知记零，也不为补齐论文级证据重新做完整训练。
3. 交付一张全量结果表、一张分组表、少量真实案例图和清楚的模型/数据路径。只跑受影响
   的直接回归和一次从保存预测复算，不重复全量测试、全盘 Hash 或 GPU 训练。

收尾完成后冻结 C3；只在发现影响输入、监督或训练有效性的具体错误时重新处理对应部分。
不继续扩充采集器、补满 19 m、追求所有指标胜出或增加训练 seed。

## C4 最小有效范围：完成一次真实辅助联合训练

保留核心方法：同一次 VLA 的共享 h、冻结的候选 ridge b、四输出残差 R，以及既定
驾驶/辅助共享梯度门控。先核验真实 candidate 与各自分支后果，不能把旧后果配给当前新
预测。四头 mask 独立；没有可核验的辅助标签时，先解决这一具体问题，不做空辅助训练。

按顺序只完成四件事：

1. 使用允许 train 数据拟合 b 和逐头尺度，检查候选/标签绑定与有效数。
2. 一个完整累积窗口的真实联合 smoke：零残差起点、非零共享梯度、配对损失、裁剪及
   checkpoint 读取正确；测量实际成本并核对总预算。其他大规模攻击/消融矩阵后置。
3. 从原始 M0 开始正式 M2，默认与 M1 同 seed、SFT 清单、顺序和 200 updates，使用
   最后有效 checkpoint；不从 M1 续训，不因结果不好重跑选优。
4. 独立加载并保存 M0/M1/M2 同清单结果，P-WP 优先，route 和失败指标同表；World 各头
   对比同数据的 b，记录真实辅助梯度与开销。没有进一步收益也保留实际结果。

当前 M1 的训练阶段记录约 289.5 s；这只能说明旧 SFT 的实际运行量，不能据此承诺 C4
总共几分钟。C4 的主要不确定性是数据绑定和联合实现，必须给交付材料保留时间。
P-WP 已较低，不预期再次出现 84% 幅度；某一明确任务的真实小幅改善可以作为 C4 亮点。
若需要缩短训练，单独登记实际预算；未达到原 T 的结果不能称原 200 步合同已完成。

## 实时闭环为硬要求：演示链前置，C5 缩矩阵但不取消现场运行

用户已确认老师必须看实时 CARLA 闭环。此前“CARLA 不可用就以离线交付收尾”的建议
不再适用；离线材料可以先完成，但不能据此认定已满足老师的展示要求。

先解决现场链路的已知缺口：

- 本次只读检查没有发现 CarlaUE4 server 进程。实际安装为 `E:\CARLA_0.9.16\CarlaUE4.exe`。
  最近 crash context（2026-09-11 09:18:56 +08:00）记录 DX12 启动通用异常
  `0xe06d7363`；异常码本身不足以确定根因。后续从日志/转储、启动上下文和依赖定位，
  不重复无变化地轮换 RHI。启动必须经统一 sim 入口，本次 RPC READY 才进入 live。
- 当前仓库启动配置 default_map 为 Town12，安装 DefaultEngine.ini 的默认地图为 Town03；
  需由统一入口对齐，记录实际地图。此差异是复现性问题，尚不能认定为崩溃根因。
- `scripts/carla_live_world_demo.py` 默认加载 H5 distilled scorer，失败回退到 H3 权重；
  不能直接称为 C4 新 World。复用已有 Runtime/控制/跟随镜头组件，显式加载原 M0 加本次
  M1 adapter/heads，并核对身份。先运行无 World 的 M1 基线，再接入真实完成的 M2/b/R。

最低现场闭环必须包含：现场相机/状态 → 本次模型实际推理 → 候选 Guard → Safety →
MPC/PID → CARLA 车辆状态反馈。由唯一 ScenarioRuntime 控制 tick；训练与渲染不并发。
先用一个允许、可重复的低复杂度场景完成约 60 s 仿真，停止/重置后再演示一次，记录
车辆运动、轨迹、实际控制来源、模型版本、干预/回退和实际推理时延。至少要有本次模型
候选实际参与控制的可核验区间；全程 Expert 回退不能称为微调模型控制成功。
现场运行不自动等于 20 Hz 模型推理，展示实测决策频率和延迟。

M2 完成后复用该演示链，不另起一套 UI/Runtime。C5 可以缩为预先指定的 1–2 个完整
配对 roots 的三臂功能示例；失败占原尝试预算。该范围不冒充原 6-root/18-run 矩阵验收，
也不据一次成功外推安全或鲁棒性。选简单场景和低画质/分辨率用于提高现场可重复性，
不使用隐藏的控制覆盖或旧 scorer 代替本次模型。录像与日志只作备用。

C6 尽早开始：先生成已有 C3 表格、架构和复现说明，M2 完成后替换/追加对应结果。
必须留下一份可打开的报告、真实权重/预测索引、演示材料以及失败/限制说明；不为添加
图表包、更多模型和额外消融推迟可交付版本。最后检查实际改动，再按已授权流程同步 GitHub。

截止已明确为周日 09-13，撤销之前给可选 C5 约 10% 的建议：先给 CARLA 与 M1 最小闭环
明确优先级，跑通后再做 M2；周日留给材料、现场复现和最终检查。若时间不足，先保证
M1 的真实现场版本，并明确 M2/三臂对照完成程度，
不宣称未完成的 C4/C5 已验收。现场仍未跑通时，如实报告该硬性交付缺项。
