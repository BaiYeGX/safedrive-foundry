# C3 修复版最终验收记录

日期：2026-09-11（运行标识中的 UTC 时间为 `20260910T165902Z`）
权威 run：`generated/h6/cora/c3-repair-20260910T165902Z/`
当前结论：`C3 VERIFIED / ENGINEERING_COMPLETED / ALGORITHM_MEASURED`
评估范围：`partial_native_support`
下一入口：`C4 NOT_RUN`

这份记录是当前 C3 的事实来源。此前 `c3-vla-sft-20260909-final-v3`、
`c3-repair-20260910T100651Z` 和更早的修复尝试仍保留在本地用于追溯，但不再作为当前
模型、指标或验收依据。

## 旧结果为什么撤回

旧适配把没有完成当前位置投影和专家来源核验的路线字段作为 route 监督，并且旧验收没有
把监督语义、恢复状态、资源监测和独立指标重算绑定在同一份身份链上。因此旧的完整验收
结论和 `90.77%` 路线改善数字不能继续使用。本次没有修改冻结 C2 release，也没有把旧数字
换成新的宣传结论；修复版重新读取原始内容、生成新的 manifest 和预测，所有数字都与当前
run 的身份绑定。

## 数据恢复与用途

修复版读取冻结 release `h6-cora-c2-devbaseline-20260907-v1`，保持 C2 的物理 root 身份
和 split：总计 211 个 root，其中 train 158、validation 53。calibration 52、
locked_development 52 和 coverage_pilot 25 只进入资产目录与审计，未进入训练或模型选择。

原生规划重建逐 root 绑定了原 anchor、规划配置、原始轨迹 Hash 和 canonical Hash。211/211
条重建与原记录身份匹配，重建错误为 0；本次使用的 reconstruction manifest 位于
`generated/h6/cora/c3-repair-20260910T163826Z/reconstruction/native-reconstruction.json`，
其内容由当前 run 的 `run_config.json` 和 `audit.json` 绑定，没有重跑 C2 release 或批量重扫
旧 Hash。

实际监督用途如下：

| 项目 | 当前事实 |
|---|---|
| route 来源 | `native_classic_planner_trajectory`；按当前位置在有序路线上的投影裁去已通过前缀，再按弧长 0…19 m、1 m 间隔取 20 点 |
| speed 来源 | `native_classic_planner_canonical`；10 个点，时间为 0.25…2.50 s |
| route 支持 | train 2173 个有效点，validation 691 个有效点；支持区间最大约 15.978 m，未将短路线外推到 19 m |
| speed 支持 | train 1580 个有效点，validation 530 个有效点；当前 canonical 10 点均有记录 |
| 训练视图 | 158 个 train root，逐头 mask；本次没有因为 route 短而删除 root |
| 验证视图 | 53 个 validation root；预测前冻结输入、真值、mask 和 root 清单 |
| zero displacement | 20 个 root 的原生 replay 为零位移，按原始事实保留，不改成向前行驶 |
| 执行记录 | 208 条无记录失败但仍待行为审计，3 条终止/介入且记录碰撞；执行后果与原生规划监督分开保存，未用一次有问题的执行分支替代规划标签 |
| 时间与速度审计 | 211 个 anchor 的当前观测速度为 0，均与 history 的 3 m/s 发生 disagreement；冲突被披露，未用未来执行速度回填；211 条执行时间线标为初态尚未完成分支内核验 |
| SFT 中的泄漏 | future label 和 World label 均为 false；导航仅作为部署输入或诊断，未自动充当专家标签 |

因此“旧数据可用”的含义是：211 个 root 的原生规划身份全部恢复，158 个 train root 和
53 个 validation root 都进入逐头有 mask 的修复版 SFT/评估视图；route 的完整 19 m
监督并不存在，当前结果必须带上 `partial_native_support` 范围。

## 补强采集结果

本次按四组固定优先序预登记了 32 train / 16 validation，共 48 个新物理 root、96 次
attempt 上限和 4 小时 CARLA 上限，并排除旧 root、保留 lineage 和禁用 seed。采集清单和
物理去重规则已保存于：

```text
generated/h6/cora/c3-repair-20260910T165902Z/supplement/manifest-plan.json
generated/h6/cora/c3-repair-20260910T165902Z/supplement/supplement-manifest.json
generated/h6/cora/c3-repair-20260910T165902Z/supplement/audit.json
```

CARLA 启动在 Windows 进程层面于 RPC READY 前以 `0xe06d7363` 崩溃，DX12、DX11、
NullRHI、Vulkan、OpenGL 和可见/隐藏启动均未取得可用 server。统一入口因此把本次采集
记录为 `CARLA_BLOCKED_EXTERNAL`：接受 0/48，attempt 开始数为 0，没有写入任何伪造图像、
轨迹或专家标签。采集清单、阻断原因和空 manifest 都通过 supplement audit；这项结果不
阻止可复算的旧数据 C3 交付，但决定了本次评估不能声称包含新增长路线或新增状态覆盖。

## 真实训练与 checkpoint

真实 batch 在 RTX 4080、CUDA、BF16 上完成。smoke 包含部署输入与可微训练输入对照、原生
forward/backward、小步更新、保存/重载、冻结指纹和 C4 成本通路测量。smoke 未被当作正式
训练权重；正式运行前重新加载原始 M0，并重置 optimizer、scheduler、RNG 和采样游标。

正式 M1 使用固定 seed 17、AdamW、LoRA 学习率 `2e-5`、驾驶头学习率 `1e-4`、weight decay
`0.01`、microbatch 1、累积 4、route/speed 各自有效坐标 SmoothL1、共同 clip 1.0，
达到冻结的 200 个 optimizer updates。158 个 root 每轮无放回，根内 anchor 确定性轮换；
完整遍历为 39 个四样本窗口加一个两样本尾窗口，实际 200 步共暴露 790 个样本，root
exposure 为 5/5/5…，尾窗口没有被丢弃或按 4 错误归一化。

smoke 与正式训练均记录显式可训练白名单。LoRA 和 route/speed 驾驶 head 发生有限有效变化，
冻结基座、视觉投影和原生 query 指纹保持不变。正式 M1 主 checkpoint 是最后一个有限有效
更新；optimizer、scheduler、RNG、epoch/root/anchor 游标、暴露次数、日志边界和累计预算
都进入 `m1_training_summary.json`。独立进程重载 checkpoint 后完成重新预测和指标重算。

C4 成本 smoke 只验证通路，没有训练 M2：真实允许标签、完整累积窗口、零残差初始化、
共享 LoRA 辅助梯度和分组裁剪均有证据；零残差输出为 0、共享 LoRA 梯度非零、裁剪后范数
均不超过 1.0。M2 后续必须从原始 M0 开始，不能从 M1 续训。

## M0/M1 评估

指标按 sample → root → root 等权聚合，配对 bootstrap 为 1000 次、seed 71；
`eps_ADE=1e-5 m` 在正式 M1 前由重复 M0/重载的数值差锁定。下面是修复版 validation
的 53 个配对 root，M0 和 M1 使用完全相同的冻结输入、真值和 mask。

| 指标 | M0 | M1 | Δ（M0−M1，正数为改善） | 相对变化 | 95% 配对 CI |
|---|---:|---:|---:|---:|---:|
| P-ADE route (m) | 1.170243 | 0.749253 | +0.420989 m | +35.9745% | [0.198651, 0.638851] |
| P-FDE route (m) | N/A | N/A | N/A | N/A | N/A |
| P-WP speed-waypoint 位置 ADE (m) | 2.178557 | 0.337217 | +1.841340 | +84.5211% | [1.544037, 2.118425] |
| P-VALID（canonical 输出） | 100.0%（53/53） | 100.0%（53/53） | — | — | — |
| P-FAIL | 0.0%（0/53） | 0.0%（0/53） | — | — | — |

P-FDE 没有用较早的最后有效点代替固定第 20 点：53 个 validation root 的第 20 个 route
真值点全部缺失，因此支持数为 0。route 完整 20 点有效率为 0%；speed 完整 10 点有效率为
100%。P-SPEED 仍为 `N/A_without_trusted_time_speed_ground_truth`，因为当前旧数据的
时间/速度真值转换口径受 anchor/history 冲突和初态核验限制，不能把 waypoint 位置误差
改名为可信速度误差。

正式 M1 相对 M0 的 route 和 speed 数字都是当前离线开发测量，可以达到本项目登记的绝对
和相对目标，但不能据此声称普遍驾驶能力提升。独立复核得到的逐 root 情况更能说明限制：
route 有 24/53 个 root 改善、29/53 个 root 退化，逐 root Δ 的中位数为 -0.019525 m，
范围为 -0.653507…+2.383661 m；speed 有 52/53 个 root 改善、1/53 个 root 退化。

## 简单诊断基线与分组结果

诊断基线只在 train 视图拟合，未使用 validation 拟合 M0/M1 主比较：

| 基线 | route ADE (m) | speed-waypoint 位置 ADE (m) | 解释 |
|---|---:|---:|---|
| train-mean | 0.748091 | 0.360589 | 训练标签的逐点均值，作为模板化诊断 |
| navigation-geometry | 0.061733 | 1.612426 | 只用部署导航 target points 的固定几何诊断 |
| M1 | 0.749253 | 0.337217 | route 几乎等于 train-mean，speed-waypoint 位置误差略优于 train-mean |

这组对照改变了结果解释：M1 的 route ADE 虽比 M0 低，但略差于 train-mean
（约 0.001162 m），明显差于 navigation-geometry；route 改善不是稳健的可观测驾驶泛化
证据。分组上，route 的 `Δ=M0−M1` 在 `free_flow`（-0.143336 m）、
`emergency_lead_brake`（-0.119475 m）、`red_light_dilemma`（-0.099017 m）和
`red_light_hold`（-0.010814 m）为负，说明这些场景退化；`stopped_lead`（+1.251303 m）
和 `cross_traffic_conflict`（+1.234783 m）改善较大。Town01/Town03/Town05 的总体 Δ
分别为 +0.247769/+0.638580/+0.374009 m，但不能掩盖场景内退化和小样本不稳定。

独立分析脚本没有调用生产聚合函数；它从保存预测和冻结 mask 重新计算 overall、分组、
逐 root delta 和 1000 次 bootstrap，`published_metric_match_errors=[]`。这使“指标可复算”
与“指标代表广泛能力”保持分开。

## 资源与环境证据

资源账本覆盖可追踪的历史失败、被撤回运行、当前 smoke、baseline、train 和 evaluate。
当前账本的优化总上界为 `0.860777642 h`，其中历史优化上界约 `2774.833178 s`，当前
优化上界约 `323.966332 s`；没有把未知消耗按零处理。正式 M1 训练 wall time 为
`289.511309 s`。整卡监测每 0.5 s 采样一次并记录 GPU UUID、采样间隙和 phase；观测到的
whole-device used peak 为 `13.6865234375 GiB`，低于 `14.5 GiB`，进程 allocated/reserved
峰值分别为 `9.889133/10.845703 GiB`。当前训练优化、历史优化和总账都低于各自登记上限。

CARLA 的补采阻断属于外部进程启动失败，不被包装成训练或数据成功；补采没有消耗 accepted
root 配额，也没有把一次 RPC 失败改成 READY。Windows 配置变更前保存了
`C:\Users\张周易\AppData\Local\CarlaUE4\Saved\Config\WindowsNoEditor\Engine.ini.c3-backup-20260911T001431`。

## 深入自检与实际命令

权威 `verify` 在独立进程中得到 `status=VERIFIED`、`errors=[]`，四个检查组
`data_semantics`、`training_loop`、`independent_metrics` 和 `resource_budget` 全部为 true。
`deep-self-check.py` 先重跑正式 verify，然后创建六个临时 hard-link 副本并在断开 hard-link
后篡改：配置 seed、manifest mask、预测数值、重复 root、GPU monitor 路径和 failure 记录。
六类攻击全部得到 `status=FAILED` 并被拒绝，完整摘要在：
`generated/h6/cora/c3-repair-20260910T165902Z/deep-self-check.json`。

本轮实际执行的主要命令为：

```bash
wsl.exe -d Ubuntu-24.04 -- /home/sdf/.venvs/sdf/bin/python scripts/h6_cora_reconstruct.py --release-root "/mnt/e/autonomous driving/generated/h6/cora/h6-cora-c2-devbaseline-20260907-v1" --output "/mnt/e/autonomous driving/generated/h6/cora/c3-repair-20260910T163826Z/reconstruction/native-reconstruction.json"
wsl.exe -d Ubuntu-24.04 -- /home/sdf/.venvs/sdf/bin/python scripts/h6_cora_collect_sft.py plan --run-id c3-repair-20260910T165902Z
wsl.exe -d Ubuntu-24.04 -- /home/sdf/.venvs/sdf/bin/python scripts/h6_cora_collect_sft.py collect --run-id c3-repair-20260910T165902Z
wsl.exe -d Ubuntu-24.04 -- /home/sdf/.venvs/sdf/bin/python scripts/h6_cora_collect_sft.py audit --run-id c3-repair-20260910T165902Z
wsl.exe -d Ubuntu-24.04 -- /home/sdf/.venvs/sdf/bin/python scripts/h6_cora_sft.py audit --run-id c3-repair-20260910T165902Z --device cuda --reconstruction-manifest /mnt/e/autonomous driving/generated/h6/cora/c3-repair-20260910T163826Z/reconstruction/native-reconstruction.json --supplement-manifest /mnt/e/autonomous driving/generated/h6/cora/c3-repair-20260910T165902Z/supplement/supplement-manifest.json
wsl.exe -d Ubuntu-24.04 -- /home/sdf/.venvs/sdf/bin/python scripts/h6_cora_sft.py smoke --run-id c3-repair-20260910T165902Z --device cuda --reconstruction-manifest /mnt/e/autonomous driving/generated/h6/cora/c3-repair-20260910T163826Z/reconstruction/native-reconstruction.json --supplement-manifest /mnt/e/autonomous driving/generated/h6/cora/c3-repair-20260910T165902Z/supplement/supplement-manifest.json
wsl.exe -d Ubuntu-24.04 -- /home/sdf/.venvs/sdf/bin/python scripts/h6_cora_sft.py baseline --run-id c3-repair-20260910T165902Z --device cuda --reconstruction-manifest /mnt/e/autonomous driving/generated/h6/cora/c3-repair-20260910T163826Z/reconstruction/native-reconstruction.json --supplement-manifest /mnt/e/autonomous driving/generated/h6/cora/c3-repair-20260910T165902Z/supplement/supplement-manifest.json
wsl.exe -d Ubuntu-24.04 -- /home/sdf/.venvs/sdf/bin/python scripts/h6_cora_sft.py train --run-id c3-repair-20260910T165902Z --device cuda --updates 200 --accumulation 4 --max-hours 4 --reconstruction-manifest /mnt/e/autonomous driving/generated/h6/cora/c3-repair-20260910T163826Z/reconstruction/native-reconstruction.json --supplement-manifest /mnt/e/autonomous driving/generated/h6/cora/c3-repair-20260910T165902Z/supplement/supplement-manifest.json
wsl.exe -d Ubuntu-24.04 -- /home/sdf/.venvs/sdf/bin/python scripts/h6_cora_sft.py evaluate --run-id c3-repair-20260910T165902Z --device cuda --reconstruction-manifest /mnt/e/autonomous driving/generated/h6/cora/c3-repair-20260910T163826Z/reconstruction/native-reconstruction.json --supplement-manifest /mnt/e/autonomous driving/generated/h6/cora/c3-repair-20260910T165902Z/supplement/supplement-manifest.json
wsl.exe -d Ubuntu-24.04 -- /home/sdf/.venvs/sdf/bin/python scripts/h6_cora_sft.py verify --run-id c3-repair-20260910T165902Z --device cpu --reconstruction-manifest /mnt/e/autonomous driving/generated/h6/cora/c3-repair-20260910T163826Z/reconstruction/native-reconstruction.json --supplement-manifest /mnt/e/autonomous driving/generated/h6/cora/c3-repair-20260910T165902Z/supplement/supplement-manifest.json
wsl.exe -d Ubuntu-24.04 -- /home/sdf/.venvs/sdf/bin/python scripts/h6_cora_deep_self_check.py --run-dir "/mnt/e/autonomous driving/generated/h6/cora/c3-repair-20260910T165902Z"
wsl.exe -d Ubuntu-24.04 -- /home/sdf/.venvs/sdf/bin/python scripts/h6_cora_independent_analysis.py --run-dir "/mnt/e/autonomous driving/generated/h6/cora/c3-repair-20260910T165902Z"
wsl.exe -d Ubuntu-24.04 -- /home/sdf/.venvs/sdf/bin/python -m unittest discover -s tests -t . -v
wsl.exe -d Ubuntu-24.04 -- /home/sdf/.venvs/sdf/bin/python -m compileall -q safedrive_foundry scripts tests
git diff --check
```

全量 unittest 实际结果为 `Ran 522 tests in 97.767s; OK (skipped=1)`；C3 focused suite 为
`Ran 27 tests in 24.381s; OK`；compileall 和 `git diff --check` 返回 0。关键产物由
`stage-summary.json`、`verify.json`、`deep-self-check.json`、
`independent-analysis.json` 和本报告的身份索引共同定位。大型模型、原始图像、训练预测和
完整本地日志仍在本机 `generated/` 下，Git 只提交可审阅代码、测试、文档和小型身份索引。

## C3 完成边界与 C4 交接

C3 的数据语义、真实训练、checkpoint 重载、逐 root 评估、资源记录和验收攻击已完成，
所以工程状态为 `VERIFIED`。算法状态单独记为 `MEASURED`：speed waypoint 在本次固定离线
任务上改善明显，route 相对 M0 改善但接近训练均值基线且有 29/53 个 root 退化；这不是
普遍驾驶能力或闭环安全结论。P-FDE、P-SPEED 和新增 CARLA 覆盖保留为 N/A/未完成项。

下一入口已切换到 C4。C4 本轮没有执行，M2 仍未训练；未来 M2 和 M1 都必须从同一个原始
M0 起点开始，不能从 M1 或 smoke checkpoint 续训。补采的 CARLA 外部阻断、旧数据初态/速度
冲突和部分 route 支持会在 C4/C6 的报告中继续作为研究限制。
